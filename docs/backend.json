
{
  "entities": {
    "Member": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Member",
      "type": "object",
      "description": "Represents a participant in the Chit Fund, including their personal details and contribution information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Member entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the chit fund member."
        },
        "phone": {
          "type": "string",
          "description": "The contact phone number of the member."
        },
        "joinDate": {
          "type": "string",
          "description": "The date when the member joined the chit fund.",
          "format": "date-time"
        },
        "monthlyAmount": {
          "type": "number",
          "description": "The fixed monthly contribution amount required from the member."
        },
        "status": {
          "type": "string",
          "description": "The current status of the member in the chit fund (e.g., 'active', 'inactive')."
        }
      },
      "required": [
        "id",
        "name",
        "phone",
        "joinDate",
        "monthlyAmount",
        "status"
      ]
    },
    "Payment": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Payment",
      "type": "object",
      "description": "Records a single monthly payment made by a member towards the chit fund.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Payment entity."
        },
        "memberId": {
          "type": "string",
          "description": "Reference to the Member who made this payment."
        },
        "memberName": {
          "type": "string",
          "description": "The name of the member for whom the payment was recorded."
        },
        "month": {
          "type": "string",
          "description": "The specific month and year for which the payment is made."
        },
        "amountPaid": {
          "type": "number",
          "description": "The actual amount of money paid."
        },
        "paymentDate": {
          "type": "string",
          "description": "The date when this payment was recorded.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The current status of the payment."
        }
      },
      "required": [
        "id",
        "memberId",
        "memberName",
        "month",
        "amountPaid",
        "paymentDate",
        "status"
      ]
    },
    "ChitRound": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ChitRound",
      "type": "object",
      "description": "Details of a completed chit fund round, including the winning member and the amount won.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ChitRound entity."
        },
        "roundNumber": {
          "type": "number",
          "description": "The sequential number identifying this specific chit round."
        },
        "winnerMemberId": {
          "type": "string",
          "description": "Reference to the Member who won this chit round."
        },
        "winnerName": {
          "type": "string",
          "description": "The name of the member who won this chit round."
        },
        "winningAmount": {
          "type": "number",
          "description": "The amount of money received by the winner."
        },
        "date": {
          "type": "string",
          "description": "The date when this chit round concluded.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "roundNumber",
        "winnerMemberId",
        "winnerName",
        "winningAmount",
        "date"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/members/{memberId}",
        "definition": {
          "entityName": "Member",
          "schema": {
            "$ref": "#/backend/entities/Member"
          },
          "description": "Stores individual member details. Accessible to all authenticated users for now.",
          "params": [
            {
              "name": "memberId",
              "description": "The unique identifier for a member."
            }
          ]
        }
      },
      {
        "path": "/payments/{paymentId}",
        "definition": {
          "entityName": "Payment",
          "schema": {
            "$ref": "#/backend/entities/Payment"
          },
          "description": "Records monthly contributions. Accessible to all authenticated users for now.",
          "params": [
            {
              "name": "paymentId",
              "description": "The unique identifier for a payment."
            }
          ]
        }
      },
      {
        "path": "/chitRounds/{roundId}",
        "definition": {
          "entityName": "ChitRound",
          "schema": {
            "$ref": "#/backend/entities/ChitRound"
          },
          "description": "Stores details of completed chit rounds. Accessible to all authenticated users for now.",
          "params": [
            {
              "name": "roundId",
              "description": "The unique identifier for a round."
            }
          ]
        }
      }
    ],
    "reasoning": "The application has moved to a simplified authorization model where any authenticated user is granted administrative privileges. This ensures immediate access to all dashboard modules (Members, Payments, Rounds, Reports) upon successful login, while maintaining Firestore structure for scalability."
  }
}
