
{
  "entities": {
    "Member": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Member",
      "type": "object",
      "description": "Represents a participant in the Chit Fund, including their personal details and contribution information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Member entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the chit fund member."
        },
        "phone": {
          "type": "string",
          "description": "The contact phone number of the member."
        },
        "joinDate": {
          "type": "string",
          "description": "The date when the member joined the chit fund.",
          "format": "date-time"
        },
        "monthlyAmount": {
          "type": "number",
          "description": "The fixed monthly contribution amount required from the member."
        },
        "status": {
          "type": "string",
          "description": "The current status of the member (e.g., 'active', 'inactive').",
          "enum": ["active", "inactive"]
        },
        "deactivatedAt": {
          "type": "string",
          "description": "Timestamp when the member was deactivated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "phone",
        "joinDate",
        "monthlyAmount",
        "status"
      ]
    },
    "Payment": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Payment",
      "type": "object",
      "description": "Records a single monthly payment made by a member towards the chit fund.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Payment entity."
        },
        "memberId": {
          "type": "string",
          "description": "Reference to the Member who made this payment."
        },
        "memberName": {
          "type": "string",
          "description": "The name of the member for whom the payment was recorded."
        },
        "month": {
          "type": "string",
          "description": "The specific month and year for which the payment is made."
        },
        "amountPaid": {
          "type": "number",
          "description": "The actual amount of money paid."
        },
        "paymentDate": {
          "type": "string",
          "description": "The date when this payment was recorded.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The current status of the payment."
        }
      },
      "required": [
        "id",
        "memberId",
        "memberName",
        "month",
        "amountPaid",
        "paymentDate",
        "status"
      ]
    },
    "ChitRound": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ChitRound",
      "type": "object",
      "description": "Details of a completed chit fund round, including the winning member and the amount won.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ChitRound entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the chit scheme."
        },
        "roundNumber": {
          "type": "number",
          "description": "The sequential number identifying this specific chit round."
        },
        "winnerMemberId": {
          "type": "string",
          "description": "Reference to the Member who won this chit round."
        },
        "winnerName": {
          "type": "string",
          "description": "The name of the member who won this chit round."
        },
        "winningAmount": {
          "type": "number",
          "description": "The amount of money received by the winner."
        },
        "date": {
          "type": "string",
          "description": "The date when this chit round concluded.",
          "format": "date-time"
        },
        "collectionType": {
          "type": "string",
          "enum": ["Daily", "Monthly"],
          "description": "The frequency of collections for this chit round."
        },
        "dailyAmount": {
          "type": "number",
          "description": "The amount collected daily if collectionType is Daily."
        }
      },
      "required": [
        "id",
        "name",
        "collectionType"
      ]
    },
    "MonthlyTarget": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "MonthlyTarget",
      "type": "object",
      "description": "A financial collection goal set by the admin for a specific month and year.",
      "properties": {
        "id": { "type": "string", "description": "Format: YYYY-MM" },
        "year": { "type": "string" },
        "month": { "type": "string" },
        "targetAmount": { "type": "number" }
      },
      "required": ["id", "year", "month", "targetAmount"]
    },
    "AuditLog": {
      "title": "AuditLog",
      "type": "object",
      "description": "History of administrative actions taken in the system.",
      "properties": {
        "id": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "adminEmail": { "type": "string" },
        "adminId": { "type": "string" },
        "actionDescription": { "type": "string" }
      },
      "required": ["timestamp", "adminEmail", "actionDescription"]
    },
    "MonthLock": {
      "title": "MonthLock",
      "type": "object",
      "description": "Represents a locked financial period.",
      "properties": {
        "id": { "type": "string", "description": "Format: YYYY-MonthName" },
        "year": { "type": "string" },
        "monthName": { "type": "string" },
        "lockedAt": { "type": "string", "format": "date-time" },
        "lockedBy": { "type": "string" }
      },
      "required": ["id", "year", "monthName", "lockedAt", "lockedBy"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/members/{memberId}",
        "definition": {
          "entityName": "Member",
          "schema": {
            "$ref": "#/backend/entities/Member"
          },
          "description": "Stores individual member details."
        }
      },
      {
        "path": "/payments/{paymentId}",
        "definition": {
          "entityName": "Payment",
          "schema": {
            "$ref": "#/backend/entities/Payment"
          },
          "description": "Records monthly contributions."
        }
      },
      {
        "path": "/chitRounds/{roundId}",
        "definition": {
          "entityName": "ChitRound",
          "schema": {
            "$ref": "#/backend/entities/ChitRound"
          },
          "description": "Stores details of completed chit rounds."
        }
      },
      {
        "path": "/monthlyTargets/{targetId}",
        "definition": {
          "entityName": "MonthlyTarget",
          "schema": {
            "$ref": "#/backend/entities/MonthlyTarget"
          },
          "description": "Stores monthly collection targets."
        }
      },
      {
        "path": "/auditLogs/{logId}",
        "definition": {
          "entityName": "AuditLog",
          "schema": {
            "$ref": "#/backend/entities/AuditLog"
          },
          "description": "Stores system audit logs."
        }
      },
      {
        "path": "/monthLocks/{lockId}",
        "definition": {
          "entityName": "MonthLock",
          "schema": {
            "$ref": "#/backend/entities/MonthLock"
          },
          "description": "Stores locked months information."
        }
      }
    ],
    "reasoning": "The application manages members, payments, chit rounds, collection targets, audit logs, and period locks."
  }
}
